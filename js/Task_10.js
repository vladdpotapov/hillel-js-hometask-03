// TASK #10:
// Напечатать полную таблицу умножения от 1 до 10.

let result;

for (let multiplier = 1, number = 1; multiplier <= 10; multiplier++) {
    result = number * multiplier;
    // document.write(result + ' | ');
    let div = document.createElement('div');
    div.innerHTML = `<hr>1 * ${multiplier} = ` + result;
    document.body.append(div);
}

for (let multiplier = 1, number = 2; multiplier <= 10; multiplier++) {
    result = number * multiplier;
    // document.write(result + ' | ');
    let div = document.createElement('div');
    div.innerHTML = `<hr>2 * ${multiplier} = ` + result;
    document.body.append(div);
}

for (let multiplier = 1, number = 3; multiplier <= 10; multiplier++) {
    result = number * multiplier;
    // document.write(result + ' | ');
    let div = document.createElement('div');
    div.innerHTML = `<hr>3 * ${multiplier} = ` + result;
    document.body.append(div);
}

for (let multiplier = 1, number = 4; multiplier <= 10; multiplier++) {
    result = number * multiplier;
    // document.write(result + ' | ');
    let div = document.createElement('div');
    div.innerHTML = `<hr>4 * ${multiplier} = ` + result;
    document.body.append(div);
}

for (let multiplier = 1, number = 5; multiplier <= 10; multiplier++) {
    result = number * multiplier;
    // document.write(result + ' | ');
    let div = document.createElement('div');
    div.innerHTML = `<hr>5 * ${multiplier} = ` + result;
    document.body.append(div);
}

for (let multiplier = 1, number = 6; multiplier <= 10; multiplier++) {
    result = number * multiplier;
    // document.write(result + ' | ');
    let div = document.createElement('div');
    div.innerHTML = `<hr>6 * ${multiplier} = ` + result;
    document.body.append(div);
}

for (let multiplier = 1, number = 7; multiplier <= 10; multiplier++) {
    result = number * multiplier;
    // document.write(result + ' | ');
    let div = document.createElement('div');
    div.innerHTML = `<hr>7 * ${multiplier} = ` + result;
    document.body.append(div);
}

for (let multiplier = 1, number = 8; multiplier <= 10; multiplier++) {
    result = number * multiplier;
    // document.write(result + ' | ');
    let div = document.createElement('div');
    div.innerHTML = `<hr>8 * ${multiplier} = ` + result;
    document.body.append(div);
}

for (let multiplier = 1, number = 9; multiplier <= 10; multiplier++) {
    result = number * multiplier;
    // document.write(result + ' | ');
    let div = document.createElement('div');
    div.innerHTML = `<hr>9 * ${multiplier} = ` + result;
    document.body.append(div);
}

for (let multiplier = 1, number = 10; multiplier <= 10; multiplier++) {
    result = number * multiplier;
    // document.write(result + ' | ');
    let div = document.createElement('div');
    div.innerHTML = `<hr>10 * ${multiplier} = ` + result;
    document.body.append(div);
}